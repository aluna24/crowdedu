## Replace indoor soccer image

Overwrite `src/assets/sport-soccer.jpg` with the newly uploaded `im-soccer.jpg`. No code changes needed — the existing import in `src/pages/Intramurals.tsx` will pick up the new file contents on rebuild.

### Step
1. Copy `user-uploads://im-soccer.jpg` → `src/assets/sport-soccer.jpg` (overwrite).